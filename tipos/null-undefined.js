"use strict";
(() => {
    //Con la restriccion ya no se puede pero con
    // stricNullChecks : false si se podrá
    let isActive = null;
    // true,false,undefined,null
    // null !== undefined
    // No son iguales !==
    console.log(isActive);
})();
